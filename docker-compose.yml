services:
  app:
    build:
      context: .
    image: kyuing-bot:latest
    container_name: kyuing-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
