services:
  app:
    build:
      context: .
    image: kyuing-bot:latest
    container_name: kyuing-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-5001}:${WEB_PORT:-5001}"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
